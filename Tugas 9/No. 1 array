#include <stdio.h>

int main() {
    // Deklarasi dan inisialisasi array
    int nilai[5] = {1, 2, 3, 4, 5};

    // Menampilkan elemen array dengan indeks ke-3
    printf("%d\n", nilai[3]);

    return 0;
}
